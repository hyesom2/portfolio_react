import WindowFrame from '@/layout/WindowFrame';

function Note() {
  return (
    <WindowFrame className="relative flex h-full flex-col pt-7">
      <h1 className="sr-only">GuestBook</h1>
      <main className="h-full flex-1 overflow-hidden">방명록 준비 중 입니다.</main>
    </WindowFrame>
  );
}

export default Note;
